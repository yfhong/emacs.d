;;; init-elpa.el --- Settings and helpers for package.el -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

;; (add-hook 'term-setup-hook 'vterm-initialize)
;; (setq terminal-frame-alist '((:terminal . vterm-mode)))
;; (setq terminal-emulator "xterm-256color")
;; (setq exec-path (cons "/usr/lib" exec-path))
(require 'vterm)

(provide 'init-vterm)
